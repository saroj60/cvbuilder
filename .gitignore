# Dependency directories
node_modules/
jspm_packages/
web_modules/

# Build outputs
dist/
build/
.cache/
.parcel-cache/
out/
.next/
scratch/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Hostinger/Server logs
*.log
hostinger-debug.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor and OS settings
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db
*.zip

# Backend Prisma generate artifact backup (optional, usually kept in project)
# prisma/migrations (don't ignore migrations, they are needed for prisma migrate)
# eng.traineddata (OCR data file, can be large, but if it is needed by backend it may be committed or loaded at runtime. Let's keep it unless it's too big)
